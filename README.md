# qooee.github.io

Hugo site for [qooee.net](https://qooee.net/), using the Ananke theme and GitHub Pages Actions deployment.

## Local development

Initialise the theme submodule, then run Hugo:

```bash
git submodule update --init --recursive
hugo server
```
